# D01-D20 完整修复执行计划（最终版 v3.0）

**制定时间**: 2026-09-08  
**修订版本**: v3.0 - 彻底严格版  
**原则**: 每步必须0 failed才能通过，绝不放水  

---

## 🚫 禁止性验收规则（一票否决）

### 以下行为**一律不算修复完成**：

1. ❌ 删除失败测试
2. ❌ 修改测试适应错误行为（除非确认测试确实过时）
3. ❌ 使用skip/xfail/条件判断隐藏失败
4. ❌ 修改业务数据库数据来配合测试
5. ❌ 关闭功能后宣称功能已修复
6. ❌ 添加空壳方法但主流程不调用
7. ❌ 只验证函数存在、文件存在
8. ❌ 只验证日志打印"成功"
9. ❌ 只通过局部测试就宣称完成
10. ❌ 将"未验证"写成"已完成"
11. ❌ 使用"至少X个通过"这种模糊标准
12. ❌ 使用"FAILED可接受"这种放宽标准

### ✅ 唯一合格的完成标准：

**所有测试 0 failed + 全量回归无新增失败 + 资源释放 + 数据隔离**

---

## 📋 统一通过标准（所有步骤）

每步通过**必须同时满足**以下10项：

1. ✅ **收集成功**: 相关测试全部被pytest收集
2. ✅ **全部执行**: 所有收集到的测试都执行
3. ✅ **0 failed**: **没有任何失败测试**
4. ✅ **skip说明**: skipped=0，或每个skip有书面理由
5. ✅ **xfail限制**: xfailed=0，除非已批准为暂不支持项
6. ✅ **无警告**: 无资源泄漏、连接未关闭等警告
7. ✅ **数据隔离**: 真实业务数据库未被污染
8. ✅ **无回归**: 之前通过的测试仍然全部通过
9. ✅ **场景完整**: 正常、异常、边界三类场景都覆盖
10. ✅ **日志完整**: 有完整测试日志和修改文件清单

**如果某项暂时无法验证，必须标记为"未验证"，不得写"已完成"**

---

## 第一批: 测试基础设施修复（6步）

### 步骤1.1: 修复全局stdout污染

**当前问题**:
```bash
pytest tests/ -q
# 输出: ValueError: I/O operation on closed file
# 文件: tests/test_phase1_fixes.py
# 位置: 全局作用域（约第10-15行）
# 原因: sys.stdout = StringIO() 导致pytest输出捕获失效
```

**修复动作**:
```python
# 文件: F:\hydro_platform_v1\tests\test_phase1_fixes.py

# 删除全局劫持:
# import sys
# from io import StringIO  
# captured_output = StringIO()
# sys.stdout = captured_output  # ❌ 删除整个块

# 如果测试需要捕获输出，改为使用pytest fixture:
def test_something_with_output(capsys):
    """使用pytest的capsys fixture捕获输出"""
    print("test output")
    captured = capsys.readouterr()
    assert "test output" in captured.out
```

**实际测试**:
```bash
cd F:/hydro_platform_v1
mkdir -p logs

# 测试1: 验证可以收集
python -m pytest tests/ --collect-only -q 2>&1 | tee logs/step1.1_collect.log

# 测试2: 统计结果
python << 'EOF' | tee logs/step1.1_analysis.log
import re
with open('logs/step1.1_collect.log', 'r', encoding='utf-8') as f:
    content = f.read()

# 提取收集数量和错误
match_tests = re.search(r'(\d+) tests? collected', content)
match_errors = re.search(r'(\d+) errors?', content)

collected = int(match_tests.group(1)) if match_tests else 0
errors = int(match_errors.group(1)) if match_errors else 0

# 检查stdout错误
has_stdout_error = 'stdout' in content.lower() or 'I/O operation' in content.lower() or 'closed file' in content.lower()

print(f"收集: {collected} tests")
print(f"错误: {errors} errors")
print(f"stdout污染: {'是' if has_stdout_error else '否'}")

# 判断通过
if collected >= 100 and errors == 0 and not has_stdout_error:
    print("\n✅ 步骤1.1通过")
    exit(0)
else:
    print(f"\n❌ 步骤1.1失败:")
    if collected < 100:
        print(f"  - 收集数量不足: {collected} < 100")
    if errors > 0:
        print(f"  - 存在收集错误: {errors}")
    if has_stdout_error:
        print(f"  - 仍有stdout污染")
    exit(1)
EOF
```

**通过标准（严格）**:
- [ ] 收集数量 >= 100
- [ ] 收集错误 = 0（**不是<= 3，必须=0**）
- [ ] 不包含 "stdout"、"I/O operation"、"closed file"
- [ ] 日志已保存到 logs/step1.1_*.log

**停止**: ✋ 等待确认通过

---

### 步骤1.2: 验证生产schema并创建严格一致的fixture

**当前问题**:
- 测试无完整schema初始化
- 可能存在"测试专用简化schema"与生产不一致

**前置验证（必须先执行）**:
```bash
# 验证1: 生产schema文件存在性
echo "=== 前置验证 ===" | tee logs/step1.2_prereq.log
ls -lh F:/hydro_platform_v1/hydro_platform/database/schema.sql >> logs/step1.2_prereq.log 2>&1

if [ ! -f "F:/hydro_platform_v1/hydro_platform/database/schema.sql" ]; then
    echo "❌ schema.sql不存在，必须先确认生产建库路径" | tee -a logs/step1.2_prereq.log
    exit 1
fi

# 验证2: schema包含必需表
echo "schema表数量:" >> logs/step1.2_prereq.log
grep -c "CREATE TABLE" F:/hydro_platform_v1/hydro_platform/database/schema.sql >> logs/step1.2_prereq.log

# 验证3: 迁移系统是否可用
python << 'EOF' | tee -a logs/step1.2_prereq.log
try:
    from hydro_platform.database.migrations import migrate, CURRENT_VERSION, _applied_version
    print(f"✓ 迁移系统可用，当前版本: v{CURRENT_VERSION}")
except Exception as e:
    print(f"❌ 迁移系统不可用: {e}")
    exit(1)
    
# 验证4: migrate()接受的参数类型
import inspect
sig = inspect.signature(migrate)
print(f"migrate()签名: {sig}")
EOF

# 如果前置验证失败，不得继续
```

**修复动作（基于前置验证结果）**:
```python
# 文件: F:\hydro_platform_v1\tests\conftest.py

import pytest
import sqlite3
from pathlib import Path

@pytest.fixture
def test_db_full_schema(tmp_path):
    """
    ⚠️ 严格要求: 必须与生产初始化完全一致
    
    不允许:
    - 维护单独的测试schema
    - 简化表结构
    - 省略索引或外键
    - 使用不同的迁移路径
    """
    db_path = tmp_path / "test.db"
    
    # 1. 使用与生产相同的连接方式
    from hydro_platform.database.connection import connect
    conn = connect(db_path)
    
    # 2. 验证schema.sql存在
    schema_file = Path(__file__).parent.parent / "hydro_platform" / "database" / "schema.sql"
    if not schema_file.exists():
        pytest.fail(f"❌ Schema文件不存在: {schema_file}")
    
    # 3. 执行生产schema
    with open(schema_file, 'r', encoding='utf-8') as f:
        schema_sql = f.read()
    
    try:
        conn.executescript(schema_sql)
    except Exception as e:
        pytest.fail(f"❌ Schema执行失败: {e}")
    
    # 4. 应用所有迁移（与生产相同）
    from hydro_platform.database.migrations import migrate, _applied_version, CURRENT_VERSION
    
    version_before = _applied_version(conn)
    
    try:
        migrate(conn)
    except Exception as e:
        pytest.fail(f"❌ 迁移失败: {e}")
    
    version_after = _applied_version(conn)
    
    # 5. 验证迁移完成
    if version_after != CURRENT_VERSION:
        pytest.fail(f"❌ 迁移版本不匹配: 期望v{CURRENT_VERSION}, 实际v{version_after}")
    
    # 6. 验证外键启用
    fk_status = conn.execute("PRAGMA foreign_keys").fetchone()[0]
    if fk_status != 1:
        pytest.fail("❌ 外键约束未启用")
    
    # 7. 验证关键表存在
    cursor = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
    )
    tables = [row[0] for row in cursor.fetchall()]
    
    required_tables = [
        'stations', 'generation_records', 'tasks', 'sources', 'documents',
        'evidence', 'extraction_candidates', 'review_items', 'review_events',
        'candidate_evidence', 'generation_record_history', 'schema_version'
    ]
    
    missing = [t for t in required_tables if t not in tables]
    if missing:
        pytest.fail(f"❌ 缺少必需表: {missing}")
    
    conn.commit()
    yield conn
    conn.close()


@pytest.fixture
def test_db_with_sample_data(test_db_full_schema):
    """
    在完整schema基础上添加最小依赖数据
    
    只创建外键依赖数据，业务数据由各测试自行创建
    """
    conn = test_db_full_schema
    
    # 插入基础站点（很多表的外键依赖）
    conn.execute("""
        INSERT INTO stations (
            entity_id, entity_type, canonical_name, country,
            capacity_mw, priority_tier, collection_priority
        ) VALUES (?, ?, ?, ?, ?, ?, ?)
    """, ('test_station_1', 'station', 'Test Station', 'CN', 1000.0, 'A', 100))
    
    # 插入基础来源（documents的外键依赖）
    conn.execute("""
        INSERT INTO sources (
            source_id, entity_id, url, title, created_at
        ) VALUES (?, ?, ?, ?, datetime('now'))
    """, ('test_source_1', 'test_station_1', 'http://test.com', 'Test Source'))
    
    conn.commit()
    return conn
```

**实际测试**:
```bash
cd F:/hydro_platform_v1

# 测试1: 创建fixture验证测试
cat > tests/test_fixture_严格验证.py << 'EOF'
"""Fixture严格验证: 确保与生产完全一致"""
import pytest

def test_fixture_schema_version_matches_production(test_db_full_schema):
    """验证fixture的schema版本与生产要求一致"""
    from hydro_platform.database.migrations import _applied_version, CURRENT_VERSION
    conn = test_db_full_schema
    
    version = _applied_version(conn)
    assert version == CURRENT_VERSION, \
        f"❌ Schema版本不匹配: 期望v{CURRENT_VERSION}, 实际v{version}"
    
    print(f"✅ Schema版本一致: v{version}")

def test_fixture_has_all_required_tables(test_db_full_schema):
    """验证fixture创建了所有必需表"""
    conn = test_db_full_schema
    cursor = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
    )
    tables = [row[0] for row in cursor.fetchall()]
    
    required = [
        'stations', 'generation_records', 'tasks', 'sources', 'documents',
        'evidence', 'extraction_candidates', 'review_items', 'review_events',
        'candidate_evidence', 'generation_record_history', 'schema_version'
    ]
    
    missing = [t for t in required if t not in tables]
    assert len(missing) == 0, f"❌ 缺少必需表: {missing}"
    
    print(f"✅ 所有{len(required)}个必需表存在")
    print(f"   实际表数: {len(tables)}")

def test_fixture_foreign_keys_enabled(test_db_full_schema):
    """验证外键约束已启用"""
    conn = test_db_full_schema
    fk_status = conn.execute("PRAGMA foreign_keys").fetchone()[0]
    assert fk_status == 1, "❌ 外键约束未启用"
    print("✅ 外键约束已启用")

def test_fixture_uses_temp_database(test_db_full_schema):
    """验证fixture使用临时数据库"""
    conn = test_db_full_schema
    cursor = conn.execute("PRAGMA database_list")
    db_path = cursor.fetchone()[2]
    
    assert 'tmp' in db_path.lower() or 'temp' in db_path.lower(), \
        f"❌ 数据库应在临时目录: {db_path}"
    
    print(f"✅ 使用临时数据库: {db_path}")

def test_sample_data_fixture_provides_dependencies(test_db_with_sample_data):
    """验证sample_data提供基础依赖"""
    conn = test_db_with_sample_data
    
    station_count = conn.execute("SELECT COUNT(*) FROM stations").fetchone()[0]
    source_count = conn.execute("SELECT COUNT(*) FROM sources").fetchone()[0]
    
    assert station_count >= 1, "❌ 应提供至少1个station"
    assert source_count >= 1, "❌ 应提供至少1个source"
    
    print(f"✅ 基础依赖数据: stations={station_count}, sources={source_count}")
EOF

# 测试2: 运行fixture验证
python -m pytest tests/test_fixture_严格验证.py -v -s 2>&1 | tee logs/step1.2_fixture_test.log

# 测试3: 统计结果（严格）
python << 'EOF' | tee logs/step1.2_analysis.log
import re
with open('logs/step1.2_fixture_test.log', 'r', encoding='utf-8') as f:
    content = f.read()

match_passed = re.search(r'(\d+) passed', content)
match_failed = re.search(r'(\d+) failed', content)

passed = int(match_passed.group(1)) if match_passed else 0
failed = int(match_failed.group(1)) if match_failed else 0

print(f"通过: {passed}")
print(f"失败: {failed}")

if passed == 5 and failed == 0:
    print("\n✅ 步骤1.2通过")
    exit(0)
else:
    print(f"\n❌ 步骤1.2失败: 必须5/5通过且0失败，实际{passed}通过{failed}失败")
    exit(1)
EOF
```

**通过标准（严格）**:
- [ ] 前置验证全部通过
- [ ] 5/5 fixture测试 PASSED（**必须全部通过**）
- [ ] 0 failed（**不接受任何失败**）
- [ ] Schema版本 = CURRENT_VERSION
- [ ] 所有必需表存在
- [ ] 外键已启用
- [ ] 使用临时数据库

**停止**: ✋ 等待确认通过

---

### 步骤1.3: 修复D18测试 - 先判断责任再修复

**当前问题**:
```bash
pytest tests/test_d18_scheduler_isolation.py
# 输出: AttributeError: 'TaskRepository' object has no attribute 'create'
```

**问题判断（必须先执行）**:
```bash
# 判断1: 检查TaskRepository当前API
python << 'EOF' | tee logs/step1.3_api_check.log
from hydro_platform.database.repositories import TaskRepository
import inspect

print("=== TaskRepository当前API ===")
methods = [m for m in dir(TaskRepository) if not m.startswith('_') and callable(getattr(TaskRepository, m))]
for m in methods:
    sig = inspect.signature(getattr(TaskRepository, m))
    print(f"  {m}{sig}")

print("\n是否有create方法:", 'create' in methods)
print("是否有upsert_many方法:", 'upsert_many' in methods)
EOF

# 判断2: 检查调度器实际调用
echo "\n=== 调度器实际调用方式 ===" >> logs/step1.3_api_check.log
grep -n "TaskRepository" F:/hydro_platform_v1/hydro_platform/app/scheduler/task_scheduler.py >> logs/step1.3_api_check.log

# 判断3: 对照设计文档
echo "\n=== 设计文档要求 ===" >> logs/step1.3_api_check.log
grep -i "TaskRepository\|create\|upsert" F:/hydro_platform_v1/docs/*.md >> logs/step1.3_api_check.log || echo "未找到设计文档要求"

# 结论: 
# - 如果生产有upsert_many，测试用create -> 测试过时，修改测试
# - 如果生产无create且设计要求有 -> 补齐生产代码
# - 如果都没有 -> 需要设计决策
```

**修复动作（假设判断结果：测试过时，应使用upsert_many）**:
```python
# 文件: F:\hydro_platform_v1\tests\test_d18_scheduler_isolation.py

from hydro_platform.models.task import Task
from hydro_platform.database.repositories import TaskRepository
from hydro_platform.common.enums import TaskType, TaskStatus
from hydro_platform.common.clock import now_iso

def test_scheduler_uses_independent_connection(test_db_full_schema):
    """测试: 调度器使用独立连接"""
    conn = test_db_full_schema
    
    # 插入基础依赖数据
    conn.execute("""
        INSERT INTO stations (entity_id, entity_type, canonical_name, country, capacity_mw, priority_tier, collection_priority)
        VALUES (?, ?, ?, ?, ?, ?, ?)
    """, ('entity_1', 'station', 'Test Station', 'CN', 1000.0, 'A', 100))
    conn.commit()
    
    # 使用正确的API创建任务
    task_repo = TaskRepository(conn)
    task = Task(
        task_id="test_task_1",
        entity_id="entity_1",
        entity_type="station",
        task_type=TaskType.STATION_GENERATION,
        target_period="2024-01",
        status=TaskStatus.PENDING,
        created_at=now_iso(),
        updated_at=now_iso()
    )
    
    # ✅ 使用实际存在的API
    task_repo.upsert_many([task])
    conn.commit()
    
    # ... 后续测试逻辑

# 修改其他两个测试函数（同样模式）
```

**实际测试**:
```bash
cd F:/hydro_platform_v1

# 测试1: 运行D18专项测试
python -m pytest tests/test_d18_scheduler_isolation.py -v -s 2>&1 | tee logs/step1.3_d18_专项.log

# 测试2: 严格统计（不接受部分通过）
python << 'EOF' | tee logs/step1.3_analysis.log
import re
with open('logs/step1.3_d18_专项.log', 'r', encoding='utf-8') as f:
    content = f.read()

match_passed = re.search(r'(\d+) passed', content)
match_failed = re.search(r'(\d+) failed', content)

passed = int(match_passed.group(1)) if match_passed else 0
failed = int(match_failed.group(1)) if match_failed else 0
total = passed + failed

print(f"D18测试结果: {passed}/{total} passed, {failed} failed")

if failed == 0 and passed == 3:
    print("\n✅ 步骤1.3通过")
    exit(0)
else:
    print(f"\n❌ 步骤1.3失败:")
    if failed > 0:
        print(f"  - 存在{failed}个失败测试")
        print(f"  - 必须修复所有失败，不得跳过或降低断言")
    if passed < 3:
        print(f"  - 未执行所有测试: {passed}/3")
    exit(1)
EOF

# 测试3: 回归测试
python -m pytest tests/test_fixture_严格验证.py -v 2>&1 | tee logs/step1.3_回归.log
grep "5 passed" logs/step1.3_回归.log || echo "❌ 回归失败"

# 测试4: 数据库污染检查
python << 'EOF' | tee logs/step1.3_db_check.log
from hydro_platform.config.paths import get_database_path
import sqlite3

prod_db = get_database_path('production')
conn = sqlite3.connect(prod_db)

test_count = conn.execute("SELECT COUNT(*) FROM tasks WHERE task_id LIKE 'test_%'").fetchone()[0]
conn.close()

if test_count > 0:
    print(f"❌ 生产库被污染: {test_count}条测试数据")
    exit(1)
else:
    print("✅ 生产库未被污染")
EOF
```

**通过标准（绝对严格 - 删除所有放宽）**:
- [ ] 问题判断已完成，责任明确
- [ ] 3/3 D18测试 PASSED（**必须全部通过，不接受2/3**）
- [ ] 0 failed（**不接受任何失败，包括test_no_connection_leak**）
- [ ] 回归测试: 5/5 fixture测试仍然PASSED
- [ ] 生产数据库未被污染

**如果test_no_connection_leak失败**:
- ❌ 不得通过降低断言（如改为">=6个"）
- ❌ 不得通过skip/xfail屏蔽
- ❌ 不得删除测试
- ✅ 必须分析失败原因并修复
- ✅ 如果短期无法修复，步骤1.3标记为"未通过"
- ✅ 不得进入步骤1.4

**停止**: ✋ 等待确认通过

---

### 步骤1.4: 修复排名测试 - 删除"至少X个"标准

**当前问题**:
```bash
pytest tests/test_generation_ranking.py
# 输出: sqlite3.OperationalError: no such table
```

**修复动作**:
```python
# 文件: F:\hydro_platform_v1\tests\test_generation_ranking.py

# 删除所有手动schema创建
# 所有测试函数改用统一fixture

def test_basic_ranking(test_db_with_sample_data):
    """测试基础排名计算"""
    conn = test_db_with_sample_data
    from hydro_platform.common.clock import now_iso
    
    # 插入测试发电记录
    conn.execute("""
        INSERT INTO generation_records (
            entity_id, period_type, period_label, generation_gwh,
            value_type, measurement_scope,
            review_status, validation_status, publication_status,
            evidence_id, created_at
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, datetime('now'))
    """, (
        'test_station_1', 'calendar_year', '2023', 98800.0,
        'actual', 'plant',
        'approved', 'passed', 'publishable',
        'evi_test_1', now_iso()
    ))
    conn.commit()
    
    # 测试排名功能
    from hydro_platform.products.generation_ranking import GenerationRanking
    ranking = GenerationRanking(conn)
    top5 = ranking.calculate_top_n(year=2023, limit=5)
    
    assert len(top5) >= 1
    assert top5[0]['generation_gwh'] == 98800.0
    print(f"✅ 排名计算正常: {len(top5)}条记录")

# ... 修复所有其他测试函数
```

**实际测试**:
```bash
cd F:/hydro_platform_v1

# 测试1: 先检查语法
python -c "
import ast
try:
    with open('tests/test_generation_ranking.py', 'r', encoding='utf-8') as f:
        ast.parse(f.read())
    print('✅ 语法检查通过')
except SyntaxError as e:
    print(f'❌ 语法错误: 第{e.lineno}行: {e.msg}')
    exit(1)
" 2>&1 | tee logs/step1.4_syntax.log

# 测试2: 运行排名测试
python -m pytest tests/test_generation_ranking.py -v -s 2>&1 | tee logs/step1.4_ranking_专项.log

# 测试3: 严格统计（删除"至少5个"标准）
python << 'EOF' | tee logs/step1.4_analysis.log
import re
with open('logs/step1.4_ranking_专项.log', 'r', encoding='utf-8') as f:
    content = f.read()

# 提取所有测试结果
tests = re.findall(r'test_\w+.*?(PASSED|FAILED)', content)
total = len(tests)
failed_tests = [t for t in tests if 'FAILED' in t]
failed_count = len(failed_tests)
passed_count = total - failed_count

print(f"排名测试结果: {passed_count}/{total} passed")

if failed_count > 0:
    print(f"\n❌ 步骤1.4失败:")
    print(f"  - 存在{failed_count}个失败测试")
    print(f"  - 失败测试:")
    for t in failed_tests:
        print(f"    * {t}")
    print(f"\n  必须修复所有失败，不得:")
    print(f"    - 删除失败测试")
    print(f"    - 降低断言")
    print(f"    - skip/xfail屏蔽")
    exit(1)
else:
    print(f"\n✅ 步骤1.4通过: 所有{total}个测试通过")
    exit(0)
EOF

# 测试4: 回归测试
python -m pytest tests/test_fixture_严格验证.py tests/test_d18_scheduler_isolation.py -v 2>&1 | tee logs/step1.4_回归.log
```

**通过标准（删除所有"至少"）**:
- [ ] 语法检查通过
- [ ] 所有测试收集成功
- [ ] **所有测试PASSED，0 failed**（不再是"至少5/8"）
- [ ] 不再出现 "no such table"
- [ ] 回归测试: fixture + D18仍然全部通过

**如果存在失败**:
- ❌ 不得删除失败测试
- ❌ 不得改为"至少X个通过"
- ✅ 必须修复或标记为"未通过"

**停止**: ✋ 等待确认通过

---

### 步骤1.5: 修复复核界面测试

**当前问题**:
```bash
pytest tests/test_review_interface.py
# 输出: sqlite3.OperationalError: no such table: stations
```

**修复动作**:
```python
# 文件: F:\hydro_platform_v1\tests\test_review_interface.py

def test_list_review_items(test_db_with_sample_data):
    """测试复核列表接口"""
    conn = test_db_with_sample_data
    from hydro_platform.common.clock import now_iso
    
    # 创建完整数据链
    conn.execute("INSERT INTO documents (document_id, source_id, file_path, created_at) VALUES (?, ?, ?, datetime('now'))",
                 ('doc_test_1', 'test_source_1', '/test.pdf'))
    
    conn.execute("""
        INSERT INTO extraction_candidates (
            candidate_id, entity_id, document_id,
            period_type, period_label, value_type, measurement_scope,
            generation_gwh, extracted_at
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    """, ('cand_test_1', 'test_station_1', 'doc_test_1',
          'calendar_year', '2023', 'actual', 'plant', 100.0, now_iso()))
    
    conn.execute("""
        INSERT INTO review_items (
            review_id, entity_id, fact_type, fact_key,
            candidate_id, reason, status, created_at
        ) VALUES (?, ?, ?, ?, ?, ?, ?, datetime('now'))
    """, ('review_test_1', 'test_station_1', 'generation', '2023',
          'cand_test_1', '待复核', 'open'))
    
    conn.commit()
    
    # 测试list接口
    from hydro_platform.app.queries import ReadQueries
    queries = ReadQueries(conn)
    items = queries.list_review_items(status='open')
    
    assert len(items) >= 1
    assert items[0]['candidate_id'] == 'cand_test_1'
    print(f"✅ 复核列表查询正常: {len(items)}项")
```

**实际测试**:
```bash
cd F:/hydro_platform_v1

python -m pytest tests/test_review_interface.py -v -s 2>&1 | tee logs/step1.5_review_专项.log

# 严格统计
python << 'EOF' | tee logs/step1.5_analysis.log
import re
with open('logs/step1.5_review_专项.log', 'r', encoding='utf-8') as f:
    content = f.read()

match_passed = re.search(r'(\d+) passed', content)
match_failed = re.search(r'(\d+) failed', content)

passed = int(match_passed.group(1)) if match_passed else 0
failed = int(match_failed.group(1)) if match_failed else 0

if failed == 0 and passed > 0:
    print(f"✅ 步骤1.5通过: {passed}个测试全部通过")
    exit(0)
else:
    print(f"❌ 步骤1.5失败: {passed} passed, {failed} failed")
    exit(1)
EOF

# 回归测试
python -m pytest tests/test_fixture_严格验证.py tests/test_d18_scheduler_isolation.py tests/test_generation_ranking.py -v 2>&1 | tee logs/step1.5_回归.log
```

**通过标准**:
- [ ] 所有测试PASSED，0 failed
- [ ] 回归测试全部通过

**停止**: ✋ 等待确认通过

---

### 步骤1.6: 全量测试验证（强化 - 不只是收集）

**当前问题**:
- 只验证收集不够，可能有skip滥用、资源泄漏等隐患

**实际测试（多层验证）**:
```bash
cd F:/hydro_platform_v1

# 测试1: 收集
python -m pytest tests/ --collect-only -q 2>&1 | tee logs/step1.6_collect.log

# 测试2: 全量执行
python -m pytest tests/ -q --tb=short 2>&1 | tee logs/step1.6_run.log

# 测试3: 综合分析
python << 'EOF' | tee logs/step1.6_comprehensive_analysis.log
import re

# 读取收集结果
with open('logs/step1.6_collect.log', 'r', encoding='utf-8') as f:
    collect_content = f.read()

match = re.search(r'(\d+) tests? collected', collect_content)
collected = int(match.group(1)) if match else 0

match = re.search(r'(\d+) errors?', collect_content)
collect_errors = int(match.group(1)) if match else 0

# 读取执行结果
with open('logs/step1.6_run.log', 'r', encoding='utf-8') as f:
    run_content = f.read()

match = re.search(r'(\d+) passed', run_content)
passed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) failed', run_content)
failed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) skipped', run_content)
skipped = int(match.group(1)) if match else 0

match = re.search(r'(\d+) xfailed', run_content)
xfailed = int(match.group(1)) if match else 0

# 检查问题
issues = []

if collected < 100:
    issues.append(f"收集数量不足: {collected} < 100")

if collect_errors > 0:
    issues.append(f"收集错误: {collect_errors} (必须=0)")

if failed > 0:
    issues.append(f"存在失败: {failed} (必须=0)")

if skipped > passed * 0.2:
    issues.append(f"skip比例过高: {skipped}/{collected} = {skipped/collected*100:.1f}% (必须<20%)")

pass_rate = passed / (passed + failed) if (passed + failed) > 0 else 0
if pass_rate < 0.8:
    issues.append(f"通过率不足: {pass_rate*100:.1f}% (必须>=80%)")

# 检查stdout污染
if 'stdout' in collect_content.lower() or 'closed file' in collect_content.lower():
    issues.append("仍有stdout污染")

print("=== 第一批修复综合分析 ===")
print(f"收集: {collected} tests, {collect_errors} errors")
print(f"执行: {passed} passed, {failed} failed, {skipped} skipped")
print(f"通过率: {pass_rate*100:.1f}%")
print()

if len(issues) == 0:
    print("✅ 第一批测试基础设施修复完成")
    exit(0)
else:
    print("❌ 第一批仍有问题:")
    for issue in issues:
        print(f"  - {issue}")
    exit(1)
EOF

# 测试4: 资源泄漏检查
echo "=== 资源泄漏检查 ===" | tee logs/step1.6_resource_check.log
grep -i "warning.*resource\|warning.*unclosed\|warning.*leak" logs/step1.6_run.log >> logs/step1.6_resource_check.log || echo "✅ 无资源泄漏警告" >> logs/step1.6_resource_check.log

# 测试5: 数据库污染检查
python << 'EOF' | tee logs/step1.6_db_pollution.log
from hydro_platform.config.paths import get_database_path
import sqlite3

prod_db = get_database_path('production')
conn = sqlite3.connect(prod_db)

tables = ['tasks', 'stations', 'review_items', 'extraction_candidates']
total_pollution = 0

for table in tables:
    try:
        count = conn.execute(f"SELECT COUNT(*) FROM {table} WHERE {table[:-1]}_id LIKE 'test_%'").fetchone()[0]
        if count > 0:
            print(f"❌ {table}: {count}条测试数据")
            total_pollution += count
    except:
        pass

conn.close()

if total_pollution > 0:
    print(f"\n❌ 生产库被污染: 共{total_pollution}条测试数据")
    exit(1)
else:
    print("✅ 生产库未被污染")
EOF
```

**通过标准（强化）**:
- [ ] 收集: >=100 tests, 0 errors（不是<=3）
- [ ] 执行: 通过率 >= 80%
- [ ] 失败: 0 failed（不接受失败）
- [ ] Skip: < 20%
- [ ] 无stdout污染
- [ ] 无资源泄漏警告
- [ ] 生产库未被污染

**停止**: ✋ 等待确认第一批通过，审查总结报告

---

由于篇幅限制，完整计划的**第二批至第五批**将继续展开，每个D项包含：
- 具体问题描述
- 涉及文件（绝对路径）
- 修复代码
- 完整测试脚本
- 严格通过标准（0 failed）
- 停止点

**下一步**:
1. 用户确认第一批6步是否符合要求
2. 如果符合，继续展开D02-D20的完整详细步骤
3. 如果仍有问题，指出需要修改的部分

---

**计划版本**: v3.0（最终严格版）  
**承诺**: 不再放水，所有步骤必须0 failed才能通过  
**完成状态**: 第一批已完整展开，等待审查后继续展开后四批  
