# D01-D20 完整修复执行计划（v3.0 - 严格版）

**制定时间**: 2026-09-08  
**修订版本**: v3.0（严格执行版）  
**原则**: 每步必须0 failed，不接受任何妥协  

---

## ⛔ 禁止性验收规则（一票否决）

以下行为**一律不能算修复完成**，违反任何一条则该步骤**不通过**：

1. ❌ 删除失败测试
2. ❌ 修改测试断言以适应错误行为
3. ❌ 使用skip/xfail隐藏失败
4. ❌ 修改业务数据库数据来配合测试
5. ❌ 关闭功能后宣称已修复
6. ❌ 添加空壳接口但主流程不调用
7. ❌ 只验证文件或函数存在
8. ❌ 只看日志打印"成功"
9. ❌ 只通过局部测试
10. ❌ 将"未验证"写成"已完成"
11. ❌ 用"至少X个通过"作为标准
12. ❌ 用"PASSED或FAILED（可接受）"作为标准

---

## ✅ 统一通过标准

**每个步骤通过必须同时满足以下10条**：

1. ✅ 相关测试全部收集成功（0 collection errors）
2. ✅ 相关测试全部执行
3. ✅ **failed = 0**（不接受任何失败）
4. ✅ skipped = 0，或每个skipped有书面理由
5. ✅ xfailed = 0，除非已批准为明确的暂不支持项
6. ✅ 无未解释的警告
7. ✅ 无真实业务数据库污染
8. ✅ 无新增回归（之前通过的仍通过）
9. ✅ 通过正常、异常、边界三类场景
10. ✅ 有完整测试日志和修改文件清单

**如果某项暂时不能验证**:
- 标记为"未验证"，不进入下一步
- 不得标记为"已修复"或"已完成"
- 必须在报告中明确说明

---

## 第一批: 测试基础设施修复

### 步骤1.1: 修复全局stdout污染

**当前问题**:
```bash
pytest tests/ -q
# 输出: ValueError: I/O operation on closed file
# 位置: tests/test_phase1_fixes.py 全局作用域
```

**修复动作**:
```python
# 文件: F:\hydro_platform_v1\tests\test_phase1_fixes.py
# 删除以下全局代码:

# import sys
# from io import StringIO
# captured_output = StringIO()
# sys.stdout = captured_output  # ❌ 删除

# 如需捕获输出，使用pytest的capsys fixture:
def test_example(capsys):
    print("output")
    captured = capsys.readouterr()
    assert "output" in captured.out
```

**实际测试**:
```bash
cd F:/hydro_platform_v1
mkdir -p logs

# 测试1: 全量收集
python -m pytest tests/ --collect-only -q 2>&1 | tee logs/step1.1_collect.log

# 测试2: 检查是否有stdout错误
grep -i "stdout\|I/O operation\|closed file" logs/step1.1_collect.log
# 期望: 无匹配（退出码非0）

# 测试3: 统计错误数
python << 'EOF' | tee logs/step1.1_analysis.log
import re
with open('logs/step1.1_collect.log', 'r', encoding='utf-8') as f:
    content = f.read()

match = re.search(r'(\d+) tests? collected', content)
collected = int(match.group(1)) if match else 0

match = re.search(r'(\d+) errors?', content)
errors = int(match.group(1)) if match else 0

print(f"收集: {collected} tests, {errors} errors")

if errors > 0:
    print(f"❌ 步骤1.1失败: 仍有{errors}个收集错误")
    exit(1)
elif 'stdout' in content.lower() or 'closed file' in content.lower():
    print("❌ 步骤1.1失败: 仍有stdout污染")
    exit(1)
elif collected < 100:
    print(f"❌ 步骤1.1失败: 收集数量过少({collected} < 100)")
    exit(1)
else:
    print(f"✓ 步骤1.1通过: {collected} tests, 0 errors")
EOF
```

**通过标准**:
- [ ] 收集成功: X tests collected (X >= 100)
- [ ] 收集错误 = 0
- [ ] 不包含 "stdout" 或 "I/O operation" 或 "closed file"
- [ ] 日志已保存: logs/step1.1_collect.log

**如果不通过**: 返回修改，不进入步骤1.2

**停止**: ✋ 等待确认步骤1.1通过

---

### 步骤1.2: 验证生产schema路径并创建统一fixture

**当前问题**:
```bash
pytest tests/test_generation_ranking.py
# 输出: sqlite3.OperationalError: no such table: generation_records
# 原因: 测试没有使用完整schema
```

**问题判断**:
测试基础设施不完整，但必须先验证生产环境的真实schema路径

**第一步: 验证生产schema存在性**

```bash
cd F:/hydro_platform_v1

# 1. 检查schema文件
if [ ! -f "hydro_platform/database/schema.sql" ]; then
    echo "❌ schema.sql不存在，步骤1.2无法继续"
    exit 1
fi

# 2. 检查schema包含的表
echo "=== Schema包含的表 ===" | tee logs/step1.2_schema_verify.log
grep -i "CREATE TABLE" hydro_platform/database/schema.sql | tee -a logs/step1.2_schema_verify.log

# 3. 统计表数量
table_count=$(grep -c "CREATE TABLE" hydro_platform/database/schema.sql)
echo "表数量: $table_count" | tee -a logs/step1.2_schema_verify.log

if [ $table_count -lt 10 ]; then
    echo "❌ schema表数量不足($table_count < 10)"
    exit 1
fi

# 4. 验证migrate函数
python << 'EOF' | tee -a logs/step1.2_schema_verify.log
from hydro_platform.database.migrations import migrate, CURRENT_VERSION
print(f"当前迁移版本: v{CURRENT_VERSION}")
print("✓ 迁移模块可导入")
EOF

# 5. 验证生产初始化流程
python << 'EOF' | tee -a logs/step1.2_schema_verify.log
from hydro_platform.database.connection import connect
from pathlib import Path
import tempfile
import sqlite3

# 创建临时数据库测试生产初始化
tmp_db = Path(tempfile.mktemp(suffix=".db"))
conn = connect(tmp_db)

# 执行schema（与生产一致）
schema_file = Path("hydro_platform/database/schema.sql")
with open(schema_file, 'r', encoding='utf-8') as f:
    conn.executescript(f.read())

# 应用迁移（与生产一致）
from hydro_platform.database.migrations import migrate
migrate(conn)

# 验证表是否存在
cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
tables = [row[0] for row in cursor.fetchall()]

required = ['stations', 'generation_records', 'tasks', 'sources', 'documents',
            'evidence', 'extraction_candidates', 'review_items', 'review_events']

missing = [t for t in required if t not in tables]
if missing:
    print(f"❌ 生产初始化后缺少表: {missing}")
    exit(1)

print(f"✓ 生产初始化成功，共{len(tables)}个表")
print(f"  必需表全部存在: {required}")

conn.close()
tmp_db.unlink()
EOF
```

**第二步: 创建与生产一致的测试fixture**

```python
# 文件: F:\hydro_platform_v1\tests\conftest.py
# 如果已存在，备份后替换；如果不存在，创建

import pytest
import sqlite3
from pathlib import Path

@pytest.fixture
def test_db_full_schema(tmp_path):
    """
    提供完整schema的临时测试数据库
    
    ⚠️ 关键约束:
    - 必须使用与生产相同的schema.sql
    - 必须应用与生产相同的migrations
    - 不允许维护单独的测试schema
    - 任何schema变更必须同时影响测试和生产
    """
    db_path = tmp_path / "test.db"
    
    # 1. 使用生产连接方式（与生产一致）
    from hydro_platform.database.connection import connect
    conn = connect(db_path)
    
    # 2. 执行生产schema（绝对路径，与生产一致）
    schema_file = Path(__file__).parent.parent / "hydro_platform" / "database" / "schema.sql"
    if not schema_file.exists():
        pytest.fail(f"❌ Schema文件不存在: {schema_file}\n步骤1.2失败")
    
    with open(schema_file, 'r', encoding='utf-8') as f:
        schema_sql = f.read()
    
    try:
        conn.executescript(schema_sql)
    except Exception as e:
        pytest.fail(f"❌ Schema执行失败: {e}\n步骤1.2失败")
    
    # 3. 应用生产迁移（与生产一致）
    from hydro_platform.database.migrations import migrate
    try:
        migrate(conn)
    except Exception as e:
        pytest.fail(f"❌ 迁移失败: {e}\n步骤1.2失败")
    
    # 4. 验证外键启用（与生产一致）
    cursor = conn.execute("PRAGMA foreign_keys")
    if cursor.fetchone()[0] != 1:
        pytest.fail("❌ 外键约束未启用\n步骤1.2失败")
    
    # 5. 验证表完整性
    cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
    tables = [row[0] for row in cursor.fetchall()]
    
    required_tables = [
        'stations', 'generation_records', 'tasks', 'sources', 'documents',
        'evidence', 'extraction_candidates', 'review_items', 'review_events',
        'candidate_evidence', 'generation_record_history'
    ]
    
    missing = [t for t in required_tables if t not in tables]
    if missing:
        pytest.fail(f"❌ Fixture缺少必需的表: {missing}\n步骤1.2失败")
    
    conn.commit()
    yield conn
    conn.close()


@pytest.fixture
def test_db_with_sample_data(test_db_full_schema):
    """
    在完整schema基础上添加最小依赖数据
    
    只创建外键依赖的基础数据，业务数据由各测试自行创建
    """
    conn = test_db_full_schema
    
    # 插入测试站点
    conn.execute("""
        INSERT INTO stations (
            entity_id, entity_type, canonical_name, country,
            capacity_mw, priority_tier, collection_priority
        ) VALUES (?, ?, ?, ?, ?, ?, ?)
    """, ('test_station_1', 'station', 'Test Station', 'CN', 1000.0, 'A', 100))
    
    # 插入测试来源（documents的外键依赖）
    conn.execute("""
        INSERT INTO sources (
            source_id, entity_id, url, title, created_at
        ) VALUES (?, ?, ?, ?, datetime('now'))
    """, ('test_source_1', 'test_station_1', 'http://test.com', 'Test Source'))
    
    conn.commit()
    return conn
```

**第三步: 验证fixture**

```bash
cd F:/hydro_platform_v1

# 创建fixture验证测试
cat > tests/test_fixture_validation.py << 'EOF'
"""验证fixture与生产初始化一致"""
import pytest

def test_fixture_has_all_required_tables(test_db_full_schema):
    """验证fixture创建了所有必需的表"""
    conn = test_db_full_schema
    cursor = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
    )
    tables = [row[0] for row in cursor.fetchall()]
    
    required = [
        'stations', 'generation_records', 'tasks', 'sources', 'documents',
        'evidence', 'extraction_candidates', 'review_items', 'review_events',
        'candidate_evidence', 'generation_record_history'
    ]
    
    missing = [t for t in required if t not in tables]
    assert len(missing) == 0, f"缺少必需的表: {missing}"
    
    print(f"✓ 所有{len(required)}个必需表都存在")
    print(f"  实际表总数: {len(tables)}")

def test_fixture_foreign_keys_enabled(test_db_full_schema):
    """验证外键约束已启用"""
    conn = test_db_full_schema
    cursor = conn.execute("PRAGMA foreign_keys")
    assert cursor.fetchone()[0] == 1, "外键约束未启用"
    print("✓ 外键约束已启用")

def test_fixture_schema_version_matches_production(test_db_full_schema):
    """验证迁移版本与生产一致"""
    conn = test_db_full_schema
    from hydro_platform.database.migrations import _applied_version, CURRENT_VERSION
    
    version = _applied_version(conn)
    assert version == CURRENT_VERSION, \
        f"迁移版本不一致: fixture v{version}, 生产 v{CURRENT_VERSION}"
    
    print(f"✓ 迁移版本与生产一致: v{version}")

def test_fixture_uses_temp_directory(test_db_full_schema):
    """验证fixture使用临时目录，不污染生产"""
    conn = test_db_full_schema
    cursor = conn.execute("PRAGMA database_list")
    db_path = cursor.fetchone()[2]
    
    assert 'tmp' in db_path.lower() or 'temp' in db_path.lower(), \
        f"数据库应在临时目录: {db_path}"
    
    print(f"✓ 使用临时数据库: {db_path}")

def test_sample_data_provides_dependencies(test_db_with_sample_data):
    """验证sample_data fixture提供基础依赖"""
    conn = test_db_with_sample_data
    
    # 验证stations
    station_count = conn.execute("SELECT COUNT(*) FROM stations").fetchone()[0]
    assert station_count >= 1, "sample_data应提供至少1个站点"
    
    # 验证sources
    source_count = conn.execute("SELECT COUNT(*) FROM sources").fetchone()[0]
    assert source_count >= 1, "sample_data应提供至少1个来源"
    
    print(f"✓ sample_data提供基础依赖")
    print(f"  stations: {station_count}, sources: {source_count}")
EOF

# 运行fixture验证
python -m pytest tests/test_fixture_validation.py -v -s 2>&1 | tee logs/step1.2_fixture_test.log

# 验证结果
python << 'EOF' | tee logs/step1.2_analysis.log
import re
with open('logs/step1.2_fixture_test.log', 'r', encoding='utf-8') as f:
    content = f.read()

match = re.search(r'(\d+) passed', content)
passed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) failed', content)
failed = int(match.group(1)) if match else 0

print(f"Fixture验证: {passed} passed, {failed} failed")

if failed > 0:
    print(f"❌ 步骤1.2失败: 仍有{failed}个测试失败")
    exit(1)
elif passed != 5:
    print(f"❌ 步骤1.2失败: 期望5个测试通过，实际{passed}个")
    exit(1)
else:
    print("✓ 步骤1.2通过: fixture与生产一致")
EOF
```

**通过标准**:
- [ ] schema.sql文件存在
- [ ] schema包含>=10个表
- [ ] 生产初始化流程可复现
- [ ] **5/5 fixture验证测试 PASSED，0 failed**
- [ ] 输出包含 "所有11个必需表都存在"
- [ ] 输出包含 "迁移版本与生产一致"
- [ ] 数据库路径包含 tmp 或 temp
- [ ] 日志已保存

**如果不通过**: 
- 如果schema.sql不存在或不完整，停止，报告问题
- 如果任何测试failed，停止，不进入步骤1.3

**停止**: ✋ 等待确认步骤1.2通过

---

### 步骤1.3: 修复D18测试 - 判断接口责任后修复

**当前问题**:
```bash
pytest tests/test_d18_scheduler_isolation.py -v
# 输出: AttributeError: 'TaskRepository' object has no attribute 'create'
```

**问题判断（关键 - 不能跳过）**:

```bash
# 第一步: 检查生产代码API
python << 'EOF' | tee logs/step1.3_api_check.log
from hydro_platform.database.repositories import TaskRepository
import inspect

print("=== TaskRepository 公开方法 ===")
methods = [m for m in dir(TaskRepository) if not m.startswith('_') and callable(getattr(TaskRepository, m, None))]
for m in methods:
    sig = inspect.signature(getattr(TaskRepository, m))
    print(f"  {m}{sig}")

if 'create' in methods:
    print("\n✓ 生产代码有create()方法")
else:
    print("\n⚠️ 生产代码没有create()方法")
EOF

# 第二步: 检查调度器实际调用
grep -n "TaskRepository\|task_repo" hydro_platform/app/scheduler/*.py | tee -a logs/step1.3_api_check.log

# 第三步: 对照设计书（手动）
echo "需要人工判断: 查看设计书中TaskRepository应该提供哪些接口" | tee -a logs/step1.3_api_check.log
```

**判断结果处理**:

**情况A: 如果设计要求有create()但生产代码缺失**
```python
# 修复生产代码: F:\hydro_platform_v1\hydro_platform\database\repositories.py
# 在TaskRepository类中添加create方法
```

**情况B: 如果设计只要求upsert_many()，测试过时**
```python
# 修复测试代码: F:\hydro_platform_v1\tests\test_d18_scheduler_isolation.py
# 将所有create()改为upsert_many()
```

**假设判断结果是情况B，修复测试**:

```python
# 文件: F:\hydro_platform_v1\tests\test_d18_scheduler_isolation.py

from hydro_platform.models.task import Task
from hydro_platform.database.repositories import TaskRepository
from hydro_platform.common.enums import TaskType, TaskStatus
from hydro_platform.common.clock import now_iso

def test_scheduler_uses_independent_connection(test_db_full_schema):
    """测试：调度器使用独立连接"""
    conn = test_db_full_schema
    
    # 创建任务（使用正确的API）
    task_repo = TaskRepository(conn)
    task = Task(
        task_id="test_task_1",
        entity_id="entity_1",
        entity_type="station",
        task_type=TaskType.STATION_GENERATION,
        target_period="2024-01",
        status=TaskStatus.PENDING,
        created_at=now_iso(),
        updated_at=now_iso()
    )
    task_repo.upsert_many([task])  # ✓ 使用存在的API
    conn.commit()
    
    # ... 后续测试逻辑

# 修改其他两个测试函数（同样模式）
```

**实际测试**:

```bash
cd F:/hydro_platform_v1

# 测试1: 运行D18专项测试
python -m pytest tests/test_d18_scheduler_isolation.py -v -s 2>&1 | tee logs/step1.3_d18_专项.log

# 测试2: 验证结果（严格）
python << 'EOF' | tee logs/step1.3_analysis.log
import re
with open('logs/step1.3_d18_专项.log', 'r', encoding='utf-8') as f:
    content = f.read()

match = re.search(r'(\d+) passed', content)
passed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) failed', content)
failed = int(match.group(1)) if match else 0

print(f"D18测试: {passed} passed, {failed} failed")

# ⚠️ 严格标准: 必须全部通过
if failed > 0:
    print(f"❌ 步骤1.3失败: 存在{failed}个失败测试")
    print("不得进入步骤1.4")
    print("必须修复失败测试后重新验证")
    exit(1)
elif passed != 3:
    print(f"❌ 步骤1.3失败: 期望3个测试，实际{passed}个")
    exit(1)
else:
    print("✓ 步骤1.3通过: 3/3测试通过")
EOF

# 测试3: 回归测试
python -m pytest tests/test_fixture_validation.py -v 2>&1 | tee logs/step1.3_回归.log
grep "5 passed" logs/step1.3_回归.log || (echo "❌ 回归失败" && exit 1)

# 测试4: 检查生产库污染
python << 'EOF' | tee logs/step1.3_db_check.log
from hydro_platform.config.paths import get_database_path
import sqlite3

prod_db = get_database_path('production')
conn = sqlite3.connect(prod_db)
test_count = conn.execute("SELECT COUNT(*) FROM tasks WHERE task_id LIKE 'test_%'").fetchone()[0]
conn.close()

if test_count > 0:
    print(f"❌ 生产库被污染: {test_count}条测试数据")
    exit(1)
else:
    print("✓ 生产库未被污染")
EOF
```

**通过标准（严格）**:
- [ ] **3/3 D18测试 PASSED，0 failed**（不接受2/3）
- [ ] 不再出现 "AttributeError: 'create'"
- [ ] 不再出现 "no such table"
- [ ] 回归测试: 5/5 fixture测试 PASSED
- [ ] 生产库污染检查: 0条测试数据
- [ ] 日志已保存

**如果test_no_connection_leak失败**:
- ❌ 步骤1.3**不通过**
- ❌ 不进入步骤1.4
- 必须分析失败原因：
  - 调度器逻辑问题？修复调度器
  - 测试设计问题？重新设计测试
  - 功能确实无法实现？标记为"D18未验证"
- 不得删除测试
- 不得降低断言（如改为">=6个"）
- 不得skip/xfail

**停止**: ✋ 等待确认步骤1.3通过

---

### 步骤1.4: 修复排名测试 - 全部通过，0失败

**当前问题**:
```bash
pytest tests/test_generation_ranking.py -v
# 输出: sqlite3.OperationalError: no such table
# 或: 语法错误（缩进）
```

**修复动作**:
```python
# 文件: F:\hydro_platform_v1\tests\test_generation_ranking.py

# 1. 删除手动创建schema的代码
# 2. 所有测试使用统一fixture

def test_basic_ranking(test_db_with_sample_data):
    """测试基础排名计算"""
    conn = test_db_with_sample_data
    
    # 插入测试数据
    conn.execute("""
        INSERT INTO generation_records (
            entity_id, period_type, period_label, generation_gwh,
            value_type, measurement_scope,
            review_status, validation_status, publication_status,
            evidence_id, created_at
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, datetime('now'))
    """, (
        'test_station_1', 'calendar_year', '2023', 98800.0,
        'actual', 'plant',
        'approved', 'passed', 'publishable',
        'evi_test_1'
    ))
    conn.commit()
    
    # 测试排名
    from hydro_platform.products.generation_ranking import GenerationRanking
    ranking = GenerationRanking(conn)
    top5 = ranking.calculate_top_n(year=2023, limit=5)
    
    assert len(top5) >= 1
    assert top5[0]['generation_gwh'] == 98800.0
    print("✓ 排名计算正常")

# 修复其他所有测试函数
```

**实际测试**:

```bash
cd F:/hydro_platform_v1

# 测试1: 语法检查
python -c "
import ast
try:
    with open('tests/test_generation_ranking.py', 'r', encoding='utf-8') as f:
        ast.parse(f.read())
    print('✓ 语法检查通过')
except SyntaxError as e:
    print(f'❌ 语法错误: 第{e.lineno}行: {e.msg}')
    exit(1)
" 2>&1 | tee logs/step1.4_syntax.log

# 测试2: 收集测试
python -m pytest tests/test_generation_ranking.py --collect-only -q 2>&1 | tee logs/step1.4_collect.log

# 测试3: 运行全部测试
python -m pytest tests/test_generation_ranking.py -v -s 2>&1 | tee logs/step1.4_ranking_专项.log

# 测试4: 严格验证结果
python << 'EOF' | tee logs/step1.4_analysis.log
import re

# 收集结果
with open('logs/step1.4_collect.log', 'r', encoding='utf-8') as f:
    collect_content = f.read()
match = re.search(r'(\d+) tests? collected', collect_content)
collected = int(match.group(1)) if match else 0

# 执行结果
with open('logs/step1.4_ranking_专项.log', 'r', encoding='utf-8') as f:
    run_content = f.read()

match = re.search(r'(\d+) passed', run_content)
passed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) failed', run_content)
failed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) skipped', run_content)
skipped = int(match.group(1)) if match else 0

print(f"排名测试: {collected} collected, {passed} passed, {failed} failed, {skipped} skipped")

# ⚠️ 严格标准
if failed > 0:
    print(f"❌ 步骤1.4失败: 存在{failed}个失败测试")
    print("必须修复所有失败测试")
    print("不得删除测试")
    print("不得降低断言")
    print("不得skip/xfail")
    exit(1)
elif passed != collected:
    print(f"❌ 步骤1.4失败: 通过数({passed})不等于收集数({collected})")
    exit(1)
elif skipped > 0:
    print(f"⚠️ 存在{skipped}个跳过测试，需要书面解释")
else:
    print(f"✓ 步骤1.4通过: {passed}/{collected}测试全部通过")
EOF

# 测试5: 回归测试
python -m pytest tests/test_fixture_validation.py tests/test_d18_scheduler_isolation.py -v 2>&1 | tee logs/step1.4_回归.log
```

**通过标准（严格）**:
- [ ] 语法检查通过（无SyntaxError）
- [ ] **X/X 测试 PASSED，0 failed**（不接受"至少5/8"）
- [ ] passed数 = collected数（无skipped，或有书面解释）
- [ ] 不再出现 "no such table"
- [ ] 回归测试: 之前通过的仍然全部通过
- [ ] 日志已保存

**如果存在failed**:
- ❌ 步骤1.4**不通过**
- 不得删除失败测试
- 不得修改断言以适配错误行为
- 必须修复代码或测试逻辑
- 如果短期无法修复，标记"排名功能未验证"，不进入步骤1.5

**停止**: ✋ 等待确认步骤1.4通过

---

### 步骤1.5: 修复复核界面测试

**当前问题**:
```bash
pytest tests/test_review_interface.py -v
# 输出: sqlite3.OperationalError: no such table: stations
```

**修复动作**: （同步骤1.4模式，使用统一fixture）

**实际测试**: （同步骤1.4模式）

**通过标准（严格）**:
- [ ] X/X 测试 PASSED，0 failed
- [ ] review_items正确关联candidate_id
- [ ] 回归测试通过

**停止**: ✋ 等待确认步骤1.5通过

---

### 步骤1.6: 全量测试验证（强化版 - 不只是收集）

**当前问题**:
- 即使能收集，可能存在资源泄漏、skip滥用、测试污染

**实际测试（多层验证）**:

```bash
cd F:/hydro_platform_v1

# 测试1: 全量收集
python -m pytest tests/ --collect-only -q 2>&1 | tee logs/step1.6_collect.log

# 测试2: 全量执行
python -m pytest tests/ -q --tb=short 2>&1 | tee logs/step1.6_run.log

# 测试3: 失败快停（验证无早期崩溃）
python -m pytest tests/ -q --maxfail=1 2>&1 | tee logs/step1.6_run_maxfail.log

# 测试4: 详细分析
python << 'EOF' | tee logs/step1.6_analysis.log
import re

# 收集
with open('logs/step1.6_collect.log', 'r', encoding='utf-8') as f:
    collect_content = f.read()

match = re.search(r'(\d+) tests? collected', collect_content)
collected = int(match.group(1)) if match else 0

match = re.search(r'(\d+) errors?', collect_content)
collect_errors = int(match.group(1)) if match else 0

# 执行
with open('logs/step1.6_run.log', 'r', encoding='utf-8') as f:
    run_content = f.read()

match = re.search(r'(\d+) passed', run_content)
passed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) failed', run_content)
failed = int(match.group(1)) if match else 0

match = re.search(r'(\d+) skipped', run_content)
skipped = int(match.group(1)) if match else 0

match = re.search(r'(\d+) xfailed', run_content)
xfailed = int(match.group(1)) if match else 0

print("=== 第一批测试基础设施总结 ===")
print(f"收集: {collected} tests, {collect_errors} errors")
print(f"执行: {passed} passed, {failed} failed, {skipped} skipped, {xfailed} xfailed")

# 通过率
if passed + failed > 0:
    pass_rate = passed / (passed + failed)
    print(f"通过率: {pass_rate*100:.1f}%")

# skip比例
if collected > 0:
    skip_rate = skipped / collected
    print(f"Skip比例: {skip_rate*100:.1f}%")

# 判断通过条件
issues = []

if collected < 100:
    issues.append(f"收集数量不足: {collected} < 100")

if collect_errors > 3:
    issues.append(f"收集错误过多: {collect_errors} > 3")

if 'stdout' in collect_content.lower() or 'closed file' in collect_content.lower():
    issues.append("仍有stdout污染")

if pass_rate < 0.8:
    issues.append(f"通过率不足: {pass_rate*100:.1f}% < 80%")

if skip_rate > 0.2:
    issues.append(f"skip比例过高: {skip_rate*100:.1f}% > 20%")

if failed > 0:
    issues.append(f"存在{failed}个失败测试（需要全部修复或明确标记未验证）")

if len(issues) == 0:
    print("\n✓ 第一批测试基础设施修复完成")
    exit(0)
else:
    print("\n❌ 第一批仍有问题:")
    for issue in issues:
        print(f"  - {issue}")
    exit(1)
EOF

# 测试5: 资源泄漏检查
grep -i "warning.*resource\|warning.*unclosed\|warning.*leak" logs/step1.6_run.log > logs/step1.6_warnings.log
if [ -s logs/step1.6_warnings.log ]; then
    echo "⚠️ 发现资源泄漏警告:" | tee -a logs/step1.6_warnings.log
    cat logs/step1.6_warnings.log
fi

# 测试6: 生产库污染检查
python << 'EOF' | tee logs/step1.6_db_pollution.log
from hydro_platform.config.paths import get_database_path
import sqlite3

prod_db = get_database_path('production')
conn = sqlite3.connect(prod_db)

test_tables = ['tasks', 'stations', 'review_items', 'extraction_candidates']
total_pollution = 0

for table in test_tables:
    try:
        cursor = conn.execute(f"SELECT COUNT(*) FROM {table} WHERE {table[:-1]}_id LIKE 'test_%'")
        count = cursor.fetchone()[0]
        if count > 0:
            print(f"❌ {table}: {count}条测试数据")
            total_pollution += count
    except:
        pass

conn.close()

if total_pollution > 0:
    print(f"\n❌ 生产库被污染，共{total_pollution}条测试数据")
    exit(1)
else:
    print("✓ 生产库未被污染")
EOF
```

**通过标准（严格）**:
- [ ] 收集: >=100 tests, <=3 errors
- [ ] 无stdout污染
- [ ] 通过率 >= 80%
- [ ] skip比例 < 20%
- [ ] 无资源泄漏警告
- [ ] 生产库未被污染
- [ ] 如有failed，已明确记录对应的D项和"未验证"状态

**如果不通过**:
- 不进入第二批
- 分析每个失败测试
- 记录到待修复清单
- 不得删除测试来提高通过率

**停止**: ✋ 等待确认步骤1.6通过，审查第一批总结

---

## 第二批至第五批完整详细版

由于字数限制，这里列出**必须包含的内容结构**：

### 第二批: 核心数据可信闭环（8步）

**步骤2.1: D02 - 复核列表从candidates读取**
- 当前问题: （具体描述）
- 问题判断: （检查queries.py）
- 修复动作: （完整代码，绝对路径）
- 实际测试: （pytest命令+结果验证）
- 通过标准: X/X PASSED, 0 failed
- 停止

**步骤2.2: D03 - 补证不触发发布**
- 当前问题: request_more_evidence可能创建generation_records
- 问题判断: 检查orchestrator.py
- 修复动作: （完整apply_review_decision代码）
- 实际测试: 4个测试用例（补证/批准/拒绝/非法action）
- 通过标准: 4/4 PASSED, 0 failed
- 停止

**步骤2.3: D04 - 统一可信过滤器**
- 当前问题: trustworthy_filter.py存在但可能未被所有出口调用
- 问题判断: 枚举所有出口（GUI/Products/CSV/JSON/Markdown）
- 修复动作: 
  - hydro_platform/products/__init__.py
  - hydro_platform/app/queries.py
  - hydro_platform/export/csv_exporter.py
  - hydro_platform/export/json_exporter.py
  - hydro_platform/export/markdown_exporter.py
- 实际测试: 5个测试用例（forecast/rejected/无evidence/各出口）
- 通过标准: 5/5 PASSED, 0 failed
- 验证: 所有出口都调用filter_trustworthy_records()
- 停止

**步骤2.4: D05 - 实体归属验证**
- 当前问题: 简单的国家匹配不足以防止误判
- 问题判断: 检查entity_attribution.py
- 修复动作: 
  - 区分matched/ambiguous/mismatch/unknown
  - 处理同名电站、别名、项目vs电站、多站表格
- 实际测试: 8个场景（同名不同国、简称、别名、多站等）
- 通过标准: 8/8 PASSED, 0 failed
- 验证: ambiguous/mismatch/unknown不自动发布
- 停止

**步骤2.5: D06 - 候选不可变对应**
- 当前问题: 候选修改后可能覆盖evidence
- 问题判断: 检查候选创建和更新流程
- 修复动作:
  - 候选修改后生成新candidate_id
  - evidence_id不可修改
  - 同一文档多值生成多候选
- 实际测试: 5个场景（候选修改/同文档多值/冲突等）
- 通过标准: 5/5 PASSED, 0 failed
- 验证: 旧候选保持不变，新候选新ID
- 停止

**步骤2.6: D07 - 防止旧审批复用**
- 当前问题: 新候选可能误用旧审批
- 问题判断: 检查approval_versioning.py
- 修复动作:
  - 候选内容变化后重新进入open
  - 版本哈希机制
  - 并发提交检查
- 实际测试: 6个场景（100→200/并发提交/旧版本等）
- 通过标准: 6/6 PASSED, 0 failed
- 验证: 旧审批不附着到新候选
- 停止

**步骤2.7: D08 - 审批事务原子性**
- 当前问题: 多表更新可能部分成功
- 问题判断: 检查approval_transaction.py
- 修复动作:
  - SAVEPOINT嵌套事务
  - review/generation/task/event一次提交
- 实际测试: 7个故障注入（写review后失败/写generation后失败等）
- 通过标准: 7/7 PASSED, 0 failed
- 验证: 失败后所有变化回滚
- 停止

**步骤2.8: D09 - 拒绝流程完整性**
- 当前问题: reject可能影响其他候选或不同步
- 问题判断: 检查orchestrator.py reject分支
- 修复动作:
  - 同步更新review_items/generation_records/tasks
  - 不影响其他候选
  - 重连验证持久化
- 实际测试: 5个场景（拒绝/重复拒绝/不影响其他等）
- 通过标准: 5/5 PASSED, 0 failed
- 验证: 拒绝后从列表消失，不进入ranking
- 停止

### 第三批: 任务和数据库可靠性（7步）

**D11-D15, D18**，每步包含完整的：
- 问题描述
- 问题判断
- 修复代码（绝对路径）
- 测试场景（正常/异常/边界）
- 通过标准（X/X PASSED, 0 failed）
- 停止点

### 第四批: 用户入口（4步）

**D01, D02（GUI）, D10**，每步包含：
- Bridge方法存在性验证
- CSV导入完整流程
- GUI真实闭环测试
- 通过标准（X/X PASSED, 0 failed）

### 第五批: 安全交付和最终验收（5步）

**D16, D17, D19, D20**，每步包含：
- Windows凭据存储实测
- EXE实际构建和干净机器运行
- 浏览器/LLM主流程接入验证
- **D20最终验收**: 
  - 全量测试0 failed
  - 20站真值验证
  - GUI/Bridge/CSV/CLI/EXE闭环
  - 无skip/xfail或有书面解释
  - 数据库迁移/回滚测试

---

## 执行方式

```bash
# 标准执行流程:
# 1. 阅读步骤X.Y的"问题判断"和"修复动作"
# 2. 执行代码修改（不跳过判断环节）
# 3. 运行"实际测试"中的所有命令
# 4. 检查"通过标准"，必须全部✓
# 5. 如果全部✓，告知"步骤X.Y通过"
# 6. 如果任何一项✗，告知"步骤X.Y失败"，停止
# 7. 不进入下一步，直到当前步骤通过
```

**计划版本**: v3.0（严格执行版）  
**完成时间预计**: 按严格标准，约50-70小时（10-14工作日）  
